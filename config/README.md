# Config Folder

This folder contains configuration files for the project.

**DO NOT COMMIT SECRETS**
- The `service_account.json` file is your Google Cloud service account key. It is excluded from git by `.gitignore` and should never be pushed to a public repository.
- Only share non-sensitive config files (like `config.yaml`).

If you need to share credentials, use a secure method outside of git.
